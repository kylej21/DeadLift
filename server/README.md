# Instructions (local server)

## Graphrag Onboarding Server

1. Pip install from `graphrag/onboarding/requirements.txt` if venv not setup
2. Activate venv
3. Navigate to `server/`
4. `uvicorn graphrag.onboarding.main:app --port 2626 --reload`

## MCP Server

idk